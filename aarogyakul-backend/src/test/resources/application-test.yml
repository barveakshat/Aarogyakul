spring:
  datasource:
    url: jdbc:h2:mem:aarogyakul;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;NON_KEYWORDS=VALUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt-secret: test-secret-test-secret-test-secret-test-secret
  storage-mode: local
  local-storage-dir: ${java.io.tmpdir}/aarogyakul-test-documents

huggingface:
  api:
    url:
    key:
